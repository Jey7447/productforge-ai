import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";

type Opportunity = { id:string; title:string; target_audience:string|null; problem:string|null; proposed_product:string|null; opportunity_score:number|null; confidence_score:number|null; demand_score:number|null; problem_intensity_score:number|null; competition_gap_score:number|null; monetization_score:number|null; specificity_score:number|null; buildability_score:number|null };
type Evidence = { source_domain:string|null; source_type:string|null };
const n=(v:number|null)=>Math.round(Number(v??0));
function strengths(o:Opportunity){const out:string[]=[];const d=[["Demand signal",o.demand_score],["Problem severity",o.problem_intensity_score],["Competition gap",o.competition_gap_score],["Monetization signal",o.monetization_score],["Specificity",o.specificity_score],["Buildability",o.buildability_score]] as const;for(const [label,v] of d.filter(([,v])=>n(v)>=70).slice(0,4))out.push(`${label} is a strong research signal (${n(v)}/100).`);if(o.target_audience)out.push(`The target audience is defined: ${o.target_audience}.`);if(o.proposed_product)out.push("The research points to a concrete product direction rather than an abstract topic.");return out.length?out:["The opportunity has enough research signal to justify a structured validation test."];}
function risks(o:Opportunity){const out:string[]=[];const d=[["demand",o.demand_score],["problem severity",o.problem_intensity_score],["competition gap",o.competition_gap_score],["monetization",o.monetization_score],["specificity",o.specificity_score],["buildability",o.buildability_score]] as const;for(const [label,v] of d.filter(([,v])=>n(v)<60).slice(0,4))out.push(`The ${label} signal is not yet strong enough (${n(v)}/100).`);if(!o.target_audience)out.push("The target audience is not specific enough yet.");if(!o.problem)out.push("The core problem statement needs stronger specificity before testing.");return out.length?out:["No major scoring weakness was detected; willingness to pay still needs real-world confirmation."];}
function recommendations(o:Opportunity){const out:string[]=[];if(n(o.demand_score)<70)out.push("Narrow the audience and test demand with a focused message.");if(n(o.problem_intensity_score)<70)out.push("Interview target users to verify the problem is important enough to solve.");if(n(o.competition_gap_score)<70)out.push("Map the strongest alternatives and identify one concrete underserved use case.");if(n(o.monetization_score)<70)out.push("Test willingness to pay before investing in a full product build.");if(n(o.specificity_score)<70)out.push("Make the promise narrower and outcome-focused.");return out.length?out.join(" "):"Run a small audience test, gather direct feedback, and confirm willingness to pay before building the full product.";}

export async function POST(request:Request){
 const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});
 const body=await request.json().catch(()=>null) as {projectId?:string;opportunityId?:string}|null;if(!body?.projectId)return NextResponse.json({error:"projectId is required"},{status:400});
 const {data:project}=await supabase.from("projects").select("id,name").eq("id",body.projectId).eq("user_id",user.id).single();if(!project)return NextResponse.json({error:"Project not found"},{status:404});
 const q=supabase.from("opportunities").select("id,title,target_audience,problem,proposed_product,opportunity_score,confidence_score,demand_score,problem_intensity_score,competition_gap_score,monetization_score,specificity_score,buildability_score").eq("project_id",project.id);
 const {data:opportunity,error}=body.opportunityId?await q.eq("id",body.opportunityId).single():await q.order("opportunity_score",{ascending:false}).limit(1).maybeSingle();if(error||!opportunity)return NextResponse.json({error:"No researched opportunity is available to validate yet. Complete research first."},{status:400});
 const o=opportunity as Opportunity;
 const {data:linked,count:linkedCount}=await supabase.from("research_evidence").select("source_domain,source_type",{count:"exact"}).eq("opportunity_id",o.id).limit(100);
 let evidence=(linked??[]) as Evidence[];let evidenceCount=linkedCount??evidence.length;
 if(!evidence.length){const {data:run}=await supabase.from("research_runs").select("id").eq("project_id",project.id).eq("status","completed").order("created_at",{ascending:false}).limit(1).maybeSingle();if(run){const {data:rows,count}=await supabase.from("research_evidence").select("source_domain,source_type",{count:"exact"}).eq("research_run_id",run.id).limit(100);evidence=(rows??[]) as Evidence[];evidenceCount=count??evidence.length;}}
 const domains=new Set(evidence.map(x=>x.source_domain).filter(Boolean));const types=new Set(evidence.map(x=>x.source_type).filter(Boolean));const overall=n(o.opportunity_score);const confidence=n(o.confidence_score);const decision:"proceed"|"refine"|"abandon"=overall>=75&&confidence>=65?"proceed":overall>=50&&confidence>=40?"refine":"abandon";
 const evidenceSummary=`Based on ${evidenceCount} evidence records across ${domains.size} source domain${domains.size===1?"":"s"} and ${types.size} source type${types.size===1?"":"s"}. Opportunity score: ${overall}/100. Research confidence: ${confidence}/100. This is a decision aid, not a guarantee of demand or revenue.`;
 const {data:report,error:reportError}=await supabase.from("validation_reports").insert({project_id:project.id,opportunity_id:o.id,decision,confidence_score:confidence,strengths:strengths(o),risks:risks(o),disproof_findings:["If target users do not consistently describe the stated problem as important, the opportunity should be refined or rejected.","If comparable solutions satisfy users without a meaningful unmet need, the competition-gap thesis is weakened.","If qualified users show little willingness to pay for the proposed outcome, the monetization thesis is disproved."],evidence_summary:evidenceSummary,recommended_changes:recommendations(o)}).select("id,decision,confidence_score,strengths,risks,disproof_findings,evidence_summary,recommended_changes,created_at").single();
 if(reportError||!report)return NextResponse.json({error:reportError?.message??"Unable to save validation report"},{status:500});
 const nextStage=decision==="proceed"?4:3;const nextStatus=decision==="proceed"?"building":"validated";
 const {error:projectError}=await supabase.from("projects").update({status:nextStatus,current_stage:nextStage,updated_at:new Date().toISOString()}).eq("id",project.id);if(projectError)return NextResponse.json({error:projectError.message},{status:500});
 return NextResponse.json({report,opportunityId:o.id},{status:201});
}
